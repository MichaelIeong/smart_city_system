module.exports = function (RED) {
    "use strict";

    function OperatorNode(config) {
        RED.nodes.createNode(this, config);
        const node = this;

        this.on("input", function (msg, send, done) {
            send(msg);
            if (done) done();
        });
    }

    RED.nodes.registerType("Operator", OperatorNode);
};