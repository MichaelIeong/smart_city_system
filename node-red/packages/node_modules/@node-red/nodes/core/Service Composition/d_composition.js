module.exports = function (RED) {
    "use strict";

    function CompositionNode(n) {
        RED.nodes.createNode(this, n);
        var node = this;

        this.compositionName = n.compositionName;
        this.space = n.space;           // spaceId
        this.spaceName = n.spaceName;   // 新增: 保存 spaceName
        this.projectId = n.projectId;

        // 发送整个 flow 给后端
        let flowNodes = [];
        RED.nodes.eachNode(function (n) {
            flowNodes.push(n);
        });
        let flowJson = JSON.stringify(flowNodes, null, 2);

        fetch('http://127.0.0.1:8080/api/services/uploadservice', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: flowJson
        });

        // 同时把 composition 信息输出
        this.on("input", function(msg, send, done) {
            msg = msg || {};
            msg.compositionName = node.compositionName;
            msg.spaceId = node.space;
            msg.spaceName = node.spaceName;
            msg.projectId = node.projectId;

            send(msg);
            done();
        });
    }

    RED.nodes.registerType("Composition", CompositionNode);
};